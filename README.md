# meu-danfe-pdf

Serviço simples para gerar/baixar DANFE (PDF) usando a API MeuDanfe.

## Configuração
1. Copie `server/.env.example` -> `server/.env` e preencha `MEU_DANFE_API_KEY`.
2. Instale dependências:
```bash
cd server
npm install
